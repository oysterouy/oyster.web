@using oyster.web;
@using demotheme;
@TemplateHelper.Config((int _loadingTimeout) => 300)
@TemplateHelper.Config((int i) => 6)
@TemplateHelper.Config((string Name) => "sdljflasdjfljwel,sd")
@TemplateHelper.Config((decimal aa) => 6.978879M)
@TemplateHelper.Route((Request) =>
{
    string path = Request.Head.Path.ToLower();
    path = path == "/" ? "/index" : path;
    if (path.Equals("/index"))
        return new Index();
    if (path.Equals("/login"))
        return new Login();
    return null;
})
@TemplateHelper.FilterBeforeRoute((request) =>
{
    return true;
})
@TemplateHelper.FilterBeforeRequest((request, response) =>
{
    return true;
})
@TemplateHelper.FilterBeforeRander((request, resp) =>
{
    return true;
}) @TemplateHelper.FilterAfterRander((request, resp) =>
{
    return true;
})
